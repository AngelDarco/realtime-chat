---
interface Props {
	title: string;
	body: string;
	href: string;
}

const { href, title, body } = Astro.props;
---

<main class="w-[300px] h-[300px] border border-white">
	<div class="messages w-full h-full block">
		<span>testing ...</span>
	</div>
	<div class="message w-full p-1 border flex">
		<input class="w-full px-2 text-rose-700" type="text" placeholder="message">
		<button class="px-1">send</button>
	</div>
</main>
